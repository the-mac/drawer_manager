name: drawer_manager
description: The Drawer Manager class has the ability to swap Scaffold body contents, using a custom provider.
version: 0.0.4
homepage: https://github.com/the-mac/drawer_manager/

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
